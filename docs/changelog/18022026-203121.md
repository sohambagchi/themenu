[fix] deprecate old stockKind labels and standardize on inventoryLabel

## Completed
- Switched app-level inventory naming from `stockKind` to `inventoryLabel` across shared types, page props, dashboard logic, and create-item payloads.
- Updated `src/app/api/items/route.ts` to require `inventoryLabel` for reads and return `410` when only deprecated `stockKind` is supplied.
- Updated item payload validation to return a clear deprecation error when `stockKind` is sent without `inventoryLabel`.
- Kept database enum values (`Prepared`, `Ingredient`) internal and mapped through `src/lib/inventoryLabels.ts`.
- Removed legacy parse support for `Prepared` / `Ingredient` labels in API-facing parsing logic.
- Updated documentation and README language to use `Menu` / `Pantry` consistently.
- Ran non-interactive type-check and logged output in `.testing/18022026-203313.log`.

## To-Dos
- Add integration tests for deprecated query behavior (`GET /api/items?stockKind=...` -> `410`).
- Add integration tests for payload validation requiring `inventoryLabel`.
- Add non-interactive ESLint configuration so `bun run lint` can run in CI.

## ADR
- API-facing contracts now use frontend domain labels only (`Menu`, `Pantry`) to eliminate dual terminology drift.
- Database schema terminology is preserved to avoid migration churn and is treated as an internal persistence detail.
