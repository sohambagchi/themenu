[fix] align backend naming with frontend labels and bootstrap docs

## Completed
- Added `src/lib/inventoryLabels.ts` as a single mapping layer between frontend labels (`Menu`, `Pantry`) and DB stock enum values (`Prepared`, `Ingredient`).
- Updated `src/app/api/items/route.ts` GET handler to parse `inventoryLabel` first and keep legacy `stockKind` compatibility.
- Updated `src/lib/inventoryApi.ts` list-fetch wrapper to query using `inventoryLabel`.
- Updated `src/lib/itemValidation.ts` to accept either `inventoryLabel` or `stockKind` from incoming item payloads.
- Added `InventoryLabel` type to shared types.
- Created Docky documentation structure and full guide set under `docs/`.
- Captured test output logs in `.testing/`.

## To-Dos
- Add a non-interactive ESLint config (`.eslintrc` / flat config) so `bun run lint` succeeds in CI and terminal automation.
- Consider exposing `inventoryLabel` in response DTOs to remove downstream conversion assumptions.
- Add integration tests for `/api/items` query parsing (`inventoryLabel` + legacy `stockKind`).

## ADR
- Kept DB enum values unchanged (`Prepared`, `Ingredient`) to avoid migration churn.
- Standardized backend request-side naming on frontend labels by introducing an explicit mapping utility rather than scattered string comparisons.
